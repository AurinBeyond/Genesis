# FIX RECOMMENDATIONS v1.0

**Task ID:** AUDIT-001
**Brand:** Pure Soul Life / Aurin Beyond
**Layer:** Validation
**Audit Date:** 2026-04-08
**Status:** Complete

---

## PRINCIPLE

> One fix per issue. Minimal change. No system break.

---

## PRIORITY ORDER

1. CRITICAL blockers (ISSUE-002, ISSUE-005)
2. HIGH — core journey breaks (ISSUE-001, ISSUE-006, ISSUE-007)
3. MEDIUM — degraded experience (ISSUE-003, ISSUE-008, ISSUE-010)
4. LOW — optimization (ISSUE-004, ISSUE-009)

---

## FIX-001 — Gumroad CTA link update (resolves ISSUE-001)

**Severity:** HIGH
**Effort:** Minimal — 1 file, 1 line

**When to execute:** After ISSUE-002 is resolved (product published on Gumroad).

**Fix:**
In `offer.html`, find the CTA element:

```
href="https://aurinbeyond.gumroad.com/"
```

Replace with the specific product URL generated by Gumroad after publishing:

```
href="https://aurinbeyond.gumroad.com/l/[product-slug]"
```

The `[product-slug]` is the URL identifier assigned by Gumroad when the product is published. It appears in the product listing URL.

**Validation:** Navigate to `offer.html` → click CTA → confirm landing on the specific product page at `aurinbeyond.gumroad.com/l/[slug]`, not the store root.

**No other files require change.**

---

## FIX-002 — Publish product on Gumroad (resolves ISSUE-002)

**Severity:** CRITICAL
**Effort:** Moderate — requires PDF creation + Gumroad setup

**Sequence (must be done in order):**

1. Create the PDF from `01-the-source-within.md` source content (see FIX-005 — do this first)
2. Log into Gumroad at `aurinbeyond.gumroad.com`
3. Create a new product with:
   - **Title:** `The Source Within — A Guide to Internal Authority`
   - **Price:** `149 NOK`
   - **Description:** Copy from `FIRST-PRODUCT.md` Section 8
   - **Post-purchase message:** Copy from `FIRST-PRODUCT.md` Section 8
   - **File:** Upload the PDF created in Step 1
4. Publish the product
5. Copy the Gumroad product URL (`aurinbeyond.gumroad.com/l/[slug]`)
6. Apply FIX-001 to update `offer.html` CTA link

**Validation:** Complete the publish checklist in `FIRST-PRODUCT.md` Section 10. All items must be checked.

**Gate:** Step 1 gate in `PRODUCT-ROADMAP.md` is only cleared after a real test purchase confirms delivery.

---

## FIX-003 — Set `success.html` as Gumroad redirect (resolves ISSUE-003)

**Severity:** MEDIUM
**Effort:** Minimal — 1 setting in Gumroad dashboard

**Fix:**
1. In Gumroad product settings → locate "Redirect URL" or "Thank you page URL" field
2. Enter the full URL of `success.html` (e.g., `https://[your-domain]/success.html`)
3. Save settings

**Validation:** Complete a test purchase → confirm user is redirected to `success.html` and not Gumroad's default thank-you page.

**No code changes required.**

---

## FIX-004 — Align `success.html` post-purchase CTA with purchased product (resolves ISSUE-004)

**Severity:** LOW
**Effort:** Minimal — 1 file, 1 section

**Fix:**
On `success.html`, update the primary post-purchase CTA so it is contextually accurate for a PDF buyer.

Option A — Update the CTA text and destination:

Replace the "Käivita 24h protokoll" CTA with:

```
Text: Kontrolli e-posti — PDF link on saadetud
Link: No link (informational instruction only) OR link to vsl.html / offer.html for context
```

Option B — Keep `protocol.html` CTA but add a brief context line before it:

```
Add above the CTA button:
"Sinu PDF juhend on e-posti teel saadetud.
Kui soovid süsteemi sügavamale minna:"
[Käivita 24h protokoll] → protocol.html
```

Option B is the minimal change. It does not require removing the protocol CTA.

**Recommended:** Option B. No link removal. One new context line added.

**Validation:** Read `success.html` as a first-time PDF buyer. Confirm the message sequence is logical and not confusing.

---

## FIX-005 — Create the PDF product file (resolves ISSUE-005)

**Severity:** CRITICAL
**Effort:** Moderate — requires PDF creation

**Fix:**

1. Open `01-the-source-within.md`
2. Convert to PDF using any of the following methods:
   - **Pandoc (command line):** `pandoc 01-the-source-within.md -o the-source-within.pdf`
   - **Markdown to PDF tool** (e.g., Typora, Obsidian, Notion export, VS Code PDF export)
   - **Manual copy into Google Docs → Export as PDF**
3. Apply formatting rules from `FIRST-PRODUCT.md` Section 6:
   - Clean text layout
   - No images required
   - One font, one layout
   - Readable on mobile and desktop
4. Add the next-step line at the end of the PDF (per `PRODUCT-001.md` Section 9):
   > *"This guide is the first layer. The full system is available in the Academy. [link to offer.html or Gumroad Academy listing]"*
5. Save the file as `the-source-within.pdf`
6. Upload to Gumroad product (as part of FIX-002)

**Validation:** Download the PDF from a test Gumroad purchase. Confirm it opens, reads correctly, and includes the next-step reference.

---

## FIX-006 — Create `core.html` to restore ARKANA-OS flow (resolves ISSUE-006)

**Severity:** HIGH
**Effort:** Moderate — requires creating a new HTML page

**Fix:**
Create `core.html` in the repository root, consistent with the ARKANA-OS visual system (`SYSTEM_RULES.md`):

- Background: `#050505`
- Text color: `rgba(255, 255, 255, 0.9)`
- Accent: `#d4af37`
- Font: `Inter`
- Reference: `style.css` or `assets/css/style.css` (resolve ISSUE-008 first to clarify which)

**Content structure:**
`core.html` is the "hub" page (referenced as "Genesis Hub" and "Keskus" in navigation). Based on context from `success.html` and `void.html`, it should:
- Function as the central navigation point of the ARKANA-OS system
- Link forward to `delta-insight.html` (or `delta.insight.html`)
- Link to accessible tools/pages in the system
- Maintain the linear flow: `void → core → delta-insight`

**Minimal starting content:**
- System title / identity header
- Navigation to next step (`delta.insight.html`)
- Navigation back (`void.html`)

**Validation:** Navigate the chain `index → void → core → delta.insight → sigma.core → sigma-protocol` end-to-end without any 404 errors.

---

## FIX-007 — Create `notes.html` to restore success.html navigation (resolves ISSUE-007)

**Severity:** HIGH
**Effort:** Low-Moderate — requires creating a new HTML page

**Fix:**
Create `notes.html` in the repository root, consistent with ARKANA-OS visual design.

**Purpose (from `success.html` context):** A notes or reflection space for clients post-purchase.

**Minimal content:**
- Page title: Notes / Märkmed
- A simple text input area or list structure for user notes
- LocalStorage to persist notes locally (consistent with `delta-insight.html` approach — see `SYSTEM_RULES.md` Section 6 protection rule)
- Back navigation to `success.html` or `index.html`

**Validation:** Click "Notes" navigation on `success.html` → confirm page loads → confirm notes area is functional → confirm navigation back works.

---

## FIX-008 — Consolidate CSS to resolve architecture inconsistency (resolves ISSUE-008)

**Severity:** MEDIUM
**Effort:** Moderate — requires updating all HTML file references

**Fix:**

Two options:

**Option A (Recommended) — Adopt root `style.css` as canonical:**
- Delete or empty `assets/css/style.css`
- Update `SYSTEM_RULES.md` to reflect that `style.css` in root is the canonical stylesheet
- No HTML file changes required (all already reference root `style.css`)

**Option B — Adopt `assets/css/style.css` as canonical (per SYSTEM_RULES.md intent):**
- Copy content of root `style.css` into `assets/css/style.css`
- Update all HTML files to reference `assets/css/style.css` instead of `style.css`
- Delete root `style.css`

**Recommended:** Option A. It requires only one file change (update the rule), not 13+ HTML file updates. The rendered output is already correct from the root `style.css`. Option B is architecturally cleaner but is a larger change.

**Validation:** Load `index.html`, `vsl.html`, `offer.html` in browser. Confirm visual design is unchanged. No CSS errors in browser console.

---

## FIX-009 — Process open TASKMASTER tasks and establish logging (resolves ISSUE-009)

**Severity:** LOW
**Effort:** Medium — requires implementing logging and reviewing tasks

**Fix:**

1. Create `updates.log` in the repository root (or specified log path per `TASKMASTER.md` rules)
2. Initialize with the current system state entry
3. Process TASKMASTER tasks sequentially per `AGENTS-CONFIG.v.2.md` execution flow:
   - Task 002: Initialize logging (create `updates.log`)
   - Task 003: Verify workflow trigger integrity
   - Task 004: Implement task prioritization logic
   - Task 005: Implement task archiving
   - Task 006: Implement failure handling and retry logic
4. Mark each task complete in TASKMASTER after execution

**Note:** Until `updates.log` exists, the failure protocol defined in `TASKMASTER.md` Section 5 cannot function.

**Validation:** Execute one TASKMASTER task. Confirm it produces a log entry in `updates.log`. Confirm TASKMASTER is updated from `- [ ]` to `- [x]`.

---

## FIX-010 — Reconcile SYSTEM-INDEX.md with actual file locations (resolves ISSUE-010)

**Severity:** MEDIUM
**Effort:** Low — documentation update only (do not move files without full audit)

**Fix:**

Option A (Recommended in the short term) — Update SYSTEM-INDEX.md to reflect reality:
- Update the index to document files as they actually exist (root-level for `STATE.md`, `MODE.md`, etc.)
- Add a note that the structure was simplified from the original spec
- This does not break any existing functionality

Option B — Migrate files to the correct directories as specified:
- Move `STATE.md`, `MODE.md`, `INPUT.md`, `OUTPUT.md`, `TASK.md` into `core/`
- Move `AGENTS-CONFIG.md` into `core/`
- Update all cross-references in other system files
- This is a larger change with risk of breaking agent file lookups

**Recommended:** Option A first. Then evaluate Option B as a separate task with full cross-reference audit.

**Validation:** After update, SYSTEM-INDEX.md accurately reflects where files exist. An agent following the index will find files where the index says they are.

---

## EXECUTION ORDER (Recommended)

```
PHASE 1 — CRITICAL (blocks launch)
  FIX-005 → Create PDF from 01-the-source-within.md
  FIX-002 → Publish product on Gumroad (requires PDF)
  FIX-001 → Update offer.html CTA with specific Gumroad product URL
    └─ System is now capable of accepting orders and delivering product

PHASE 2 — HIGH (breaks navigation)
  FIX-006 → Create core.html (restores ARKANA-OS flow)
  FIX-007 → Create notes.html (restores success.html navigation)
    └─ All navigation chains are restored

PHASE 3 — LAUNCH VALIDATION
  Run real test purchase → confirm end-to-end delivery
  FIX-003 → Set success.html as Gumroad redirect target
    └─ Post-purchase experience is complete

PHASE 4 — MEDIUM (architecture and UX)
  FIX-008 → Consolidate CSS architecture (Option A)
  FIX-010 → Update SYSTEM-INDEX.md to reflect actual structure

PHASE 5 — LOW (optimization)
  FIX-004 → Add context line to success.html post-purchase CTA
  FIX-009 → Process open TASKMASTER tasks and initialize logging
```

---

## WHAT MUST BE FIXED BEFORE LIVE

| Fix | Required before live? |
|-----|-----------------------|
| FIX-001 | YES — conversion fails without correct link |
| FIX-002 | YES — no checkout possible |
| FIX-005 | YES — no deliverable exists |
| FIX-003 | STRONGLY RECOMMENDED |
| FIX-006 | YES for ARKANA-OS; not for PSL business flow |
| FIX-007 | YES for post-purchase navigation |
| FIX-004 | NO — acceptable for first launch |
| FIX-008 | NO — current CSS works functionally |
| FIX-009 | NO — acceptable for first launch |
| FIX-010 | NO — documentation only |

---

STATUS: Complete // Pure Soul Life // GENESIS Fix Recommendations
